# 更新日誌

## [0.1.0] - Step 1: 基本的新增、完成、刪除、未完成計數、localStorage 保存

### 新增
- 新增待辦事項輸入欄位與新增按鈕。
- 新增待辦項目的完成狀態，支援勾選完成。
- 新增待辦項目的刪除功能。
- 新增未完成項目計數顯示，顯示為「未完成:N 項」。
- 新增 localStorage 資料保存，讓待辦資料在重新整理後保留。

### 變更
- 調整待辦項目顯示方式，完成後會加上刪除線並淡化文字。
- 更新空狀態提示，當清單為空時顯示「還沒有任何待辦事項,新增一個吧!」。
- 優化新增時的輸入驗證，避免輸入空白內容造成空項目。

## [0.2.0] - Step 2: 深色模式切換(含記住偏好與跟隨系統設定)、全部/未完成/已完成篩選

### 新增
- 新增深色模式切換按鈕，位於標題列右側。
- 新增主題偏好儲存，使用者的深色/淺色選擇會記錄到 localStorage。
- 新增跟隨作業系統深淺色設定的判斷邏輯，若使用者未手動切換，會依照 prefers-color-scheme 自動套用。
- 新增全部、未完成、已完成三種篩選按鈕。
- 新增篩選後的空狀態提示文字，依據不同篩選狀態顯示對應訊息。

### 變更
- 重構 CSS 變數以支援淺色與深色主題切換，避免在各處寫死顏色。
- 調整介面樣式，使深色模式下的卡片、輸入框、按鈕和文字仍保有良好對比度。
- 更新待辦列表渲染邏輯，讓篩選結果只顯示符合條件的項目。
- 調整底部未完成計數邏輯，計數永遠依據整體待辦資料計算，不受目前篩選結果影響。
